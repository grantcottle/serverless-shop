import React from "react";
import "../../../CSS/components/Header/Header.css";

const Header = (props) => {
  return (
      <nav className="header navbar navbar-light bg-white container-fluid">
        <span className="navbar-brand mb-0">
        </span>
      </nav>
  );
};

export default Header;